import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Budgets | Finance Tracker",
  description: "Set and track your monthly spending limits. Monitor budget progress and stay on top of your finances.",
};

export { default } from "./budgets-client";
